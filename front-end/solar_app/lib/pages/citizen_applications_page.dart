import 'package:flutter/widgets.dart';

class CitizenApplicationsPage extends StatelessWidget {
  const CitizenApplicationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('applications page'),
    );
  }
}
